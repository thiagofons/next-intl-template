// Note that `app/[locale]/[...rest]/page.tsx`
// is necessary for this page to render.

/** Rendered when user tries to navigate to a 
    route that doesn't exist. */
export { default } from '@/components/NotFoundPage';

